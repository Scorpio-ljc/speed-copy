{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/socket.io/(.*)", "dest": "/server/index.js" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
} 